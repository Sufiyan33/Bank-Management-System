package com.bank.web.controller;

import org.springframework.web.bind.annotation.ControllerAdvice;

@ControllerAdvice
public class GlobalException {

	/*
	 * Add below methods so that we can catch exception when comes in actual code.
	 * 
	 * 1- handleNotFoundException
	 * 2- handleUnauthorizedException
	 * 3- handleInvalidJwtTokenException
	 * 4- handleInsufficientBalanceException
	 * 5- handleOtpRetryLimitExceededException
	 * 6- handleInvalidOTPException
	 */
}
